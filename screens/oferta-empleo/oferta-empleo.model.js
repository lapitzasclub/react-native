export class OfertaEmpleo {
  constructor(
    id,
    title,
    place,
    slots,
    contract,
    term
  ) {
    this.id = id;
    this.title = title;
    this.place = place;
    this.slots = slots;
    this.contract = contract;
    this.term = term;
  }
}
